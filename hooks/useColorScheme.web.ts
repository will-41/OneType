export function useColorScheme(): 'light' | 'dark' {
  // Web implementation - default to light theme
  // In a real app, you might want to check localStorage or system preferences
  return 'light';
}